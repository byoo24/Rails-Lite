



<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Music App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
      </li>

      <% if logged_in? %>
        <span class="navbar-text">
            <%= @user.email %>
        </span>

        <form class="form-inline" action="<%= session_url %>" method="POST">
            <input type="hidden" name="_method" value="DELETE">
            <%= auth_token %>

            <input type="submit" class="btn btn-outline-danger" value="Log Out!">
        </form>

      <% else %>

        <li class="nav-item">
            <a class="nav-link" href="<%= new_user_url %>">Register</a>
        </li>

        <li class="nav-item">
            <a class="nav-link" href="<%= new_session_url %>">Sign In</a>
        </li>

      <% end %>
    </ul>
  </div>
</nav>